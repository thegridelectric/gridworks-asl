seed_request.yaml
# Python-generated files
__pycache__/
*.py[oc]
*.DS_Store

build/
dist/
wheels/
*.egg-info
scratch.*
# Virtual environments
.venv
.ssotme
output/


*.zfs
code_gen/GridworksCore/SMQL/*

.vscode
effortless.env

# start.sh runtime artifacts
.logs/

# Claude Code worktree clones (each carries its own .git/)
.claude/worktrees/

# App-level secrets (per-process .env, magic-links tenant record).
# Tracked example/config lives in .env.example next to each .env.
app/**/.env
.env
.secrets/

**/node_modules/**/*

# Local-only gwta snapshot tooling (kept out of git by request)
gwta_seed_request.yaml
build_gwta_snapshot.sh
