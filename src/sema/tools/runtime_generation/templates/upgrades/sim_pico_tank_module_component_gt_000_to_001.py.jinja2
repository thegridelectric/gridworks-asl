    def upgrade(self) -> {{ sim_pico_tank_module_component_gt_001_class_name }}:
        """
        - ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case); SimulatesVersion 011 -> 012. Context-dependent: the device type lived on the referenced cac, not the component.
        """
        raise SemaType.upgrade_requires_context(
            "{{ sim_pico_tank_module_component_gt_000_class_name }} cannot be upgraded to "
            "{{ sim_pico_tank_module_component_gt_001_class_name }} without the source layout "
            "context: DeviceType is derived from the cac the component referenced, which "
            "the standalone component does not carry."
        )
