# GENERATED FILE — DO NOT EDIT
# Generated from indexes/public_registry.yaml


types:
  ads.channel.config:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
          - CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning)
      "000":
        summary: "Initial Sema version (flattened from the scada AdsChannelConfig runtime; ThermistorMakeModel -> ThermistorDeviceType)."

  ads111x.based.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada Ads111xBasedComponentGt runtime)."

  ads111x.based.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (specialized device-type record; per-family successor to the field ads111x.based.cac.gt, keyed by DeviceType)."

  atn.bid:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "Initial Sema version"

  bid:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  capture.tuning:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version — the per-channel capture params (CapturePeriodS, AsyncCapture, AsyncCaptureDelta, PollPeriodMs) extracted from the channel-config family into operational-params."

  channel.config:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
      "000":
        summary: "Initial Sema version"

  channel.readings:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - ChannelId: remove
          - additionalProperties: true -> false
      "001":
        summary: "Baseline runtime version (no prior version tracked)."

  channel.readings.list.item:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  component.attribute.class.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version (flattened from the scada ComponentAttributeClassGt runtime)."

  connectivity.edge.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  cop.curve:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (Intercept, OatCoeff, LwtCoeff, Min, MinOatF) — the COP model migrated out of scada deployment config into operational params."

  data.channel.gt:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - InPowerMetering: drop (metering is declared by the consuming transactive channel, not here)
          - PowerMeteringConstraint axiom: drop
      "002":
        summary: >
          - Quantity: add
          - TelemetryName: spaceheat.telemetry.name:006 -> 007
          - TelemetryQuantityConsistency axiom: add
      "001":
        summary: "Baseline runtime version (no prior version tracked)."

  derived.channel.gt:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - OutputUnit: gw1.unit 000 -> 001
          - OutputUnit: required
          - OutputQuantity: gw1.quantity 000 -> 001
          - OutputQuantity: add
          - OutputUnitQuantityConsistency axiom: add
      "001":
        summary: >
          - InputChannelNames[]: add
          - EmissionMethod: add
          - Parameters: add (Optional)
      "000":
        summary: "Initial Sema version"

  dfr.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada DfrComponentGt runtime)."

  dfr.config:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
          - CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning)
      "000":
        summary: "Initial Sema version (flattened from the scada DfrConfig runtime)."

  egauge.register.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada EgaugeRegisterConfig runtime)."

  electric.meter.cac.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version (flattened from the scada ElectricMeterCacGt runtime)."

  electric.meter.channel.config:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
          - CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning)
      "000":
        summary: "Initial Sema version (flattened from the scada ElectricMeterChannelConfig runtime)."

  electric.meter.component.gt:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "001":
        summary: "Initial Sema version (flattened from the scada ElectricMeterComponentGt runtime)."

  electric.meter.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (specialized device-type record; replaces electric.meter.cac.gt category data, keyed by DeviceType)."

  energy.instruction:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  flo.params.house0:
    latest_version: "007"
    versioning_strategy: "literal"
    versions:
      "007":
        summary: "- FloGitCommit: add"
      "006":
        summary: >
          - StabilityWeight -> StabilityPenaltyWeight: rename
          - StabilityDecay -> StabilityPenaltyDecay: rename
          - StabilityThresholdKwh -> StabilityPenaltyThresholdKwh: rename
          - StabilityHorizonHours -> StabilityPenaltyHorizonHours: rename
          - MaxHpKwhTh: add
          - MaxLoadKwhTh: add
          - RswtPenaltyEnabled: add
          - StabilityPenaltyEnabled: add
          - RswtPenaltyWeight: add
          - RswtPenaltyDecay: add
          - RswtPenaltyExponentRate: add
          - RswtPenaltyDecayMaxHour: add
      "005":
        summary: >
          - StabilityWeight: add
          - StabilityDecay: add
          - StabilityThresholdKwh: add
          - StabilityHorizonHours: add
          - PreviousPlanHpKwhElList: add (optional)
          - PreviousEstimateStorageKwhNow: add (optional)
      "004":
        summary: >
          - FloAlias: remove
          - FloGitCommit: remove
          - ConstantDeltaT: optional -> required
      "003":
        summary: "Initial Sema version"

  fsm.atomic.report:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - ActionType: remove
          - Action: scalar -> unconstrained object payload
      "000":
        summary: "Initial Sema version"

  fsm.event:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  fsm.full.report:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- AtomicList[]: fsm.atomic.report:000 -> 001"
      "000":
        summary: "Initial Sema version"

  g.node.cmd.ack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  g.node.cmd.nack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  g.node.create.cmd:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- NewNode rebinds to g.node.gt:006; new axiom 1 LocationlessAtCreation (NewNode.PositionPointId SHALL be null — a location identity is registered after creation)"
      "000":
        summary: "Initial Sema version"

  g.node.forest:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- Nodes rebind to g.node.gt:006; SendTimeMs joins required"
      "001":
        summary: "- SendTimeMs: new optional sender-clock stamp (epoch ms) at forest assembly; the registry stamps wall-clock — it is never simulated (sender-time standard, first adopter)"
      "000":
        summary: "Initial Sema version"

  g.node.forest.request:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  g.node.gt:
    latest_version: "006"
    versioning_strategy: "literal"
    versions:
      "006":
        summary: "- Axiom 2 (PhysicalGNodeLocations) becomes activation-conditioned: PositionPointId is mandatory only when Status is Active — a Pending physical GNode may be locationless"
      "005":
        summary: "adds axiom 6 GNodeAliasHasBody; no field changes"
      "004":
        summary: "Baseline runtime version (no prior version tracked)."

  g.node.instance.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  g.node.reparent.cmd:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- NewNode rebinds to g.node.gt:006"
      "001":
        summary: "- Proof: new optional opaque authorization artifact for this re-parent (format is machinery of the authority substrate, mirroring g.node.create.cmd)"
      "000":
        summary: "Initial Sema version"

  glitch:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gpio.relay.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. Board-resident per-relay GPIO component: required BoardComponentId (-> scada.board.component.gt), GpioName (-> the board record's NativeGpioOutputs), ConfigList of exactly one relay.control.config; no DeviceType (TypeName is the kind, the board component carries the board identity)."

  gpio.sensor.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. Board-resident per-sensor GPIO component, the input sibling of gpio.relay.component.gt: required BoardComponentId (-> scada.board.component.gt), GpioName (-> the board record's NativeGpioInputs), SenseMode; binary 1/0 sense; no DeviceType (TypeName is the kind, the board component carries the board identity); no SendToDerived (derived routing is computed from DerivedChannel InputChannelNames); no ConfigList (channel binds via DataChannel, capture tuning in operational params). Replaces gw108.gpio.sensor.component.gt."

  gridworks.ack:
    versioning_strategy: "none"
    summary: "Versionless acknowledgement message."

  gridworks.event.problem:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version"

  gridworks.header:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial published version, matching the gwproto Header wire format deployed in the field."

  gridworks.ping:
    versioning_strategy: "none"
    summary: "Versionless message used for liveness/heartbeat checks."

  gw:
    versioning_strategy: "none"
    summary: "Versionless GridWorks message envelope (gwproto Message wire format)."

  gw.channel.gap.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.channel.jump.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.channel.noise.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.experiment.run:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.house0.layout:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "House0-fleet layout shape laid out (mirrors gw.nolan.layout, new DeviceType model, House0 component set: eGauge / ADS111x TSnap / Krida / DFRobot / pico flow / tank + sim-tank / Hubitat + poller / web-server). All collections + Hydronic required (mirrors gw.nolan.layout) — a layout is a complete deployable artifact."

  gw.house0.operational.params:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "ScadaAlias (the applying Scada's identity — stored/emitted instances self-identify; the layout holds the full GNodes) + CaptureTuningList (per-channel capture params migrated out of the channel-config family) + the control/optimization block migrated out of scada deployment config: ActuationAuthority x ServiceMode (authority dominates; ServiceMode inert outside Active), SeasonalStorageMode, CopCurve, HeatingCurve, HpTurnOnMinutes, ShortCycleBuffer, LoadOverestimationPercent, OilBoilerBackup, HorizonHours (all required — no assumed defaults). Pairs with its layout by family."

  gw.hydronic:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Typed hydronic block promoted out of gw.house0.layout's freeform Hydronic: Zones (gw1.hvac.zone list), optional ZoneCallCircuits (gw1.zone.call.circuit list), TotalStoreTanks, UseSiegLoop, SiegLoopPlumbed, PrimaryFlowSource (gw.house0.primary.flow.source), Strategy. Axioms: SiegLoopControlImpliesPlumbed, Cardinality, CircuitResolution, LearnedNeedsTempChannel."

  gw.native.gpio.pin:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Name, BcmPin)."

  gw.nolan.layout:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Un-drafted for hardware-layout-pass-one (full layouts now, axioms later). Axioms stashed to stash_axioms.md and stripped from 000.yaml; Components are the cac-free DeviceType versions; DeviceTypes holds the specialized <family>.device.type.gt records (electric.meter / ads111x.based / gw1.scada), now non-draft; relay and opto components are the board-generic gpio.relay.component.gt / gpio.sensor.component.gt (GpioName against the board's native-pin registry). Nodes/channels ride spaceheat.node.gt/303 + data.channel.gt/003 (post-InPowerMetering-fold, matching axiom 1)."

  gw.nolan.operational.params:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version: ScadaAlias (the applying Scada's identity — stored/emitted instances self-identify) + CaptureTuningList + ActuationAuthority x ServiceMode (authority dominates; ServiceMode inert outside Active) + OnPeakWindows (gw.tou.window with explicit Days) + HeldCircuitPositions. Starts from what Nolan control consumes; store knobs and COP/heating curves join when Nolan heating-season control needs them."

  gw.readings:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.tou.window:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version: Start (inclusive) and End (exclusive) hh.mm plus Days (day.of.week list). Axioms: Start strictly before End (no midnight wrap); Days non-empty and duplicate-free."

  gw.weather.channel.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.cmd.ack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.cmd.nack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.create.cmd:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.forecast:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.forecast.bundle.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.forecast.channel.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.location.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.observation:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw1.hvac.zone:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- New shared zone type (Name, Critical, KwhPerDegF, optional TempChannelName) replacing the former parallel ZoneList / CriticalZoneList / ZoneKwhPerDegFList in the hydronic block."

  gw1.scada.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Board descriptor (DeviceType, DisplayName, MinPollPeriodMs, TelemetryNameList, NativeGpio, Expanders, Muxes, I2cRelays, SupportsPinReadback, CtAdc, ThermistorAdcs, Dacs; MuxConsistency axiom joins BusMembership / ExpanderMembership / BoardIdentifierUniqueness). The record describes what is allowed; deployment choices (DIP-selected expander address, chosen wiring) live on the component and layout."

  gw1.simple.sim.layout:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Minimal baseline stub (TypeName + Version only) to seed the layout round-trip loop; built up bit by bit toward the simulated plant's needs."

  gw1.tank.temp.calibration:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - Depth{1,2,3}B: number -> integer (offset in the OutputUnit/FahrenheitX100 scaling
            domain, mirroring linear.one.dimensional.calibration/001)
      "000":
        summary: "Initial Sema version"

  gw1.tank.temp.calibration.map:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- Buffer/Tank now reference gw1.tank.temp.calibration:001 (integer B)"
      "000":
        summary: "Initial Sema version"

  gw1.telemetry.name.quantity.projection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw1.unit.quantity.projection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw1.zone.call.circuit:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Circuit record (CircuitPosition, ServesZone, ActuatorKind, Role, CanCool, SetpointSource, Thermostat, WhitewireChannelName, FailsafeRelayNode, OpsRelayNode); FloorLoopsCannotCool and ReadSetpointNeedsCommsStat axioms."

  gw1.zone.thermostat:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Thermostat record (Kind, optional ComponentId); NoComponentOnDumbStat axiom."

  gw108.gpio.sensor.component.gt:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "001":
        summary: "Initial Sema version (flattened from the scada Gw108GpioSensorComponentGt runtime)."

  gw108.vdc.relay.component.gt:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "001":
        summary: "Initial Sema version (flattened from the scada Gw108GpioRelayComponentGt runtime, TypeName gw108.vdc.relay.component.gt)."

  ha1.params:
    latest_version: "006"
    versioning_strategy: "literal"
    versions:
      "006":
        summary: >
          - HpMaxKwEl: add as optional
          - HpMaxKwTh: required -> optional
          - HpTurnOnMinutes: add
      "005":
        summary: >
          - CopIntercept: add as optional
          - CopOatCoeff: add as optional
          - CopLwtCoeff: add as optional
          - CopMin: add as optional
          - CopMinOatF: add as optional
      "004":
        summary: "Baseline runtime version (no prior version tracked)."

  heartbeat.a:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version."

  heating.curve:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (AlphaTimes10, BetaTimes100, GammaEx6, IntermediatePowerKw, IntermediateRswtF, DdPowerKw, DdRswtF, DdDeltaTF, MaxEwtF) — the house thermal model migrated out of scada deployment config into operational params."

  heating.forecast:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  hourly.electricity.dataset:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version: HourList (utc.iso8601.seconds), PriceUsdPerKwhList, UsageKwhList."

  hp.control.box.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (retires the HpModel enum-plus-settings pattern; per-family device-type record keyed by DeviceType)."

  hp.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (retires the HpModel enum-plus-settings pattern; per-family device-type record keyed by DeviceType; absorbs HpMaxKwEl as MaxKwEl)."

  hubitat.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada HubitatComponentGt runtime)."

  hubitat.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Flattened from the scada HubitatGt runtime; derived URL helpers excluded. MacAddress moved type:string -> $ref formats/mac.address (lowercase-hex constraint; edited in place, not yet web-published)."

  hubitat.poller.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada HubitatPollerComponentGt runtime)."

  hubitat.poller.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada HubitatPollerGt runtime)."

  i2c.bit.address:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (I2cAddress, RegisterIndex, BitIndex)."

  i2c.bus:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Name, BusNumber)."

  i2c.ct.interface.capability:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. CT sibling of i2c.thermistor.interface.capability: Name, I2cBus, I2cAddress, AdcType, AdcReferenceVolts, Channels. Replaces the generic i2c.adc.capability (deleted; staging-only, single referrer)."

  i2c.dac.capability:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (DacName, I2cBus, I2cAddress, optional MuxName/MuxChannel naming the board mux the DAC sits behind, DacType, Channels; MuxPairing axiom); renames i2c.dac.config — the record describes what the board offers, not a chosen configuration."

  i2c.dac.channel.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (DacChannel, PowerOnRawValue, PowerOnVref, PowerOnGain; EepromRanges axiom)."

  i2c.dac.writer.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (ComponentId, BoardComponentId, DacName, ConfigList of i2c.dac.channel.config, DisplayName, HwUid; DacChannelUniqueness axiom). Context-dependent identity mirrors the thermistor reader: DacName and BoardComponentId resolve against the source layout."

  i2c.expander:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (ExpanderIdx, I2cBus, I2cAddress, AllowedI2cAddressList; Axiom 1 AddressSpecification: exactly one of the two address fields)."

  i2c.multichannel.dt.relay.component.gt:
    latest_version: "005"
    versioning_strategy: "literal"
    versions:
      "005":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "004":
        summary: "- ConfigList[]: relay.actor.config:002 -> 003"
      "003":
        summary: "- Add I2cBus"
      "002":
        summary: "Baseline runtime version (no prior version tracked)."

  i2c.mux:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (MuxName, I2cBus, I2cAddress, MuxType, Channels)."

  i2c.read.bit:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Bus, Address, TriggerId)."

  i2c.read.bytes:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (Bus, I2cAddress, NumBytes, TriggerId)."

  i2c.read.reg:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Bus, Address, NumBytes, TriggerId)."

  i2c.reg.address:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (I2cAddress, RegisterIndex)."

  i2c.relay.capability:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (RelayName, ExpanderIdx, RegisterIndex, BitIndex, SupportedWiringConfigs, Notes). Replaces i2c.relay.config: 'capability' says what is possible, not a chosen configuration; the expander I²C address moves to the board's i2c.expander entry (fixed or DIP-selectable), the chosen address to the component."

  i2c.relay.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. Board-resident per-relay expander component: required BoardComponentId (-> scada.board.component.gt), RelayName (-> the board record's I2cRelays), ConfigList of exactly one relay.control.config; no DeviceType (TypeName is the kind, the board component carries the board identity)."

  i2c.relay.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (RelayName, I2cBus, Address, SupportedWiringConfigs, Notes)."

  i2c.result:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Operation ref moves to i2c.operation/001; optional Bytes list for multi-byte (ReadBytes) results."
      "000":
        summary: "Initial draft (Bus, Operation, Value, Success, Error, UnixTimeMs, TriggerId)."

  i2c.thermistor.channel.config:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
          - CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning)
          - SendToDerived: drop (derived routing is computed from DerivedChannel InputChannelNames)
      "001":
        summary: "Structural-only restamp of the field-emitted shape (see 000)."
      "000":
        summary: "Reinstated 2026-08-06 from wire evidence: deployed layouts emit Version 000 records (the lineage was begun at 001 in the belief 000 was never emitted). Field-identical to 001 — upgrade is a version restamp."

  i2c.thermistor.interface.capability:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (Name, I2cBus, I2cAddress, AdcType, SupportedDataRatesSps — the conversion-rate menu the reader's DataRateSps must come from, AdcReferenceVolts, SeriesResistanceKOhms); renames i2c.thermistor.interface.config — the record describes what the board offers, not a chosen configuration."

  i2c.thermistor.reader.component.gt:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: "- Bus, AdcAddress, AdcReferenceVolts, SeriesResistanceKOhms dropped (facts on the board's thermistor-interface capability entry); AdcName added, naming that entry in the board record's ThermistorAdcs; DataRateSps added (the per-conversion data rate the reader writes in the ADC config word — a member of the board ADC's SupportedDataRatesSps); DeviceType dropped (TypeName is the kind; board identity lives on the board component); BoardComponentId added, anchoring the reader to its scada.board.component.gt. Context-dependent: AdcName and BoardComponentId are derived from the source layout, which the standalone component does not carry."
      "002":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "001":
        summary: "Structural-only restamp of the field-emitted shape (see 000; gwsproto ConfigUniqueness/AddressValidity axioms deferred)."
      "000":
        summary: "Reinstated 2026-08-06 from wire evidence: deployed layouts (spruce, July deploy) emit Version 000 records (the lineage was begun at 001 in the belief 000 was never emitted). Field-identical to 001 with 000-stamped ConfigList children — upgrade is a recursive version restamp."

  i2c.write.bit:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Bus, Address, Value, TriggerId)."

  i2c.write.byte:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (Bus, I2cAddress, Value, TriggerId)."

  i2c.write.reg:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Bus, Address, NumBytes, Value, TriggerId)."

  keyparam.change.log:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  latest.price:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  layout.lite:
    latest_version: "015"
    versioning_strategy: "literal"
    versions:
      "015":
        summary: "- TankModuleComponents / FlowModuleComponents / I2cRelayComponent: cac-carrying component versions -> cac-free DeviceType versions (pico.tank.module 011->012, sim.pico.tank.module 000->001, pico.flow.module 000->001, i2c.multichannel.dt.relay 004->005). Context-dependent: the embedded components' DeviceType lives on their cac, not the component."
      "014":
        summary: "- ShNodes: spaceheat.node.gt:301 -> 302"
      "013":
        summary: "- I2cRelayComponent: i2c.multichannel.dt.relay.component.gt:003 -> 004"
      "012":
        summary: >
          - DerivedChannels[]: derived.channel.gt:000 | 001 -> 001
          - DataChannels[]: data.channel.gt:001 -> 002
          - Ha1Params: ha1.params:004 | 005 | 006 -> ha1.params:006
          - I2cRelayComponent: i2c.multichannel.dt.relay.component.gt:002 -> 003
          - ShNodes[]: spaceheat.node.gt:300 | 301 -> 301
      "011":
        summary: >
          - Ha1Params: ha1.params:004 | 005 -> ha1.params:004 | 005 | 006
          - DerivedChannels[]: derived.channel.gt:000 -> 000 | 001
      "010":
        summary: >
          - ShNodes[]: spaceheat.node.gt:300 -> 300 | 301
          - I2cRelayComponent: required -> optional
      "009":
        summary: >
          - BufferShortCycling: add
          - Ha1Params: 004 -> 004 | 005
      "008":
        summary: >
          - SystemMode: add
          - SeasonalStorageMode: add
          - ShNodes[]: spaceheat.node.gt:200 -> 300
          - TankModuleComponents[]: pico.tank.module.component.gt only -> pico.tank.module.component.gt | sim.pico.tank.module.component.gt
          - DerivedNodeConsistency axiom: add
      "007":
        summary: "Baseline runtime version (no prior version tracked)."

  linear.one.dimensional.calibration:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - B: number -> integer
          - Semantics: clarify OutputUnit scaling domain
      "000":
        summary: "Initial Sema version"

  machine.states:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  maker.api.attribute.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada MakerAPIAttributeGt runtime; snake_case fields re-cased to CamelCase)."

  market.product:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  new.command.tree:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- ShNodes: spaceheat.node.gt:301 -> 303"
      "001":
        summary: "- ShNodes: spaceheat.node.gt:301 only (drop the multi-version oneOf; retroactive registration of the wire version the spruce scada emitted from 2026-01-28)"
      "000":
        summary: "Initial Sema version"

  operating.state.sequence:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version: ChannelName (spaceheat.name), ValueList (pascal.case), TimestampList (utc.iso8601.millis)."

  pico.btu.meter.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component.
          - FlowMeterType: spaceheat.make.model enum value -> gw1.device.type value (pascal.case); the device-type enum is articulated by the hardware layout.
      "000":
        summary: "Initial Sema version (flattened from the scada PicoBtuMeterComponentGt runtime)."

  pico.flow.module.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component.
          - FlowMeterType: spaceheat.make.model enum value -> gw1.device.type value (pascal.case); the device-type enum is articulated by the hardware layout.
      "000":
        summary: "Initial Sema version"

  pico.tank.module.component.gt:
    latest_version: "012"
    versioning_strategy: "literal"
    versions:
      "012":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "011":
        summary: "Baseline runtime version (no prior version tracked)."

  position.point.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  power.watts:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  price.quantity.unitless:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Baseline runtime version (no prior version tracked)."

  relay.actor.config:
    latest_version: "004"
    versioning_strategy: "literal"
    versions:
      "004":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
          - CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning)
      "003":
        summary: "- AsyncCaptureDelta: require when AsyncCapture is true"
      "002":
        summary: "Baseline runtime version (no prior version tracked)."

  relay.control.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. relay.actor.config minus RelayIdx: board-resident relays are identified by name against the board (gw1.scada.device.type.gt), not by a positional index."

  report:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: "- FsmReportList[]: fsm.full.report:000 -> 001"
      "002":
        summary: "Baseline runtime version (no prior version tracked)."

  report.event:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - Report: report:002 -> 003
          - Axioms: remove former ReportIdentityPropagation and ReportCreatedTimePropagation
      "002":
        summary: "Baseline runtime version (no prior version tracked)."

  scada.board.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. ComponentId, DeviceType (gw1.device.type board kind), optional I2cAddressList (field-chosen addresses, index-aligned with the record's Expanders), DisplayName, HwUid."

  scada.control.capabilities:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - RelayNodes[]: spaceheat.node.gt:300 -> 303
          - DacNodes[]: spaceheat.node.gt:300 -> 303
          - ControlChannels[]: data.channel.gt:001 -> 003
      "001":
        summary: >
          - RelayNodes[]: inline object -> spaceheat.node.gt:300
          - DacNodes[]: inline object -> spaceheat.node.gt:300
          - ControlChannels[]: inline object -> data.channel.gt:001
          - Axioms: add control surface consistency checks
      "000":
        summary: "Initial Sema version"

  scada.params:
    latest_version: "005"
    versioning_strategy: "literal"
    versions:
      "005":
        summary: >
          - NewParams: ha1.params:004 | ha1.params:005 -> ha1.params:006
          - OldParams: ha1.params:004 | ha1.params:005 -> ha1.params:006
      "004":
        summary: "Initial Sema version"

  send.control.capabilities:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  send.layout:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - FromName: remove
          - ToName: remove
          - MessageCreatedMs: add
      "000":
        summary: "Initial Sema version"

  setpoint.belief:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Belief pair (Phase, optional ValueF); UnknownIsValueless axiom ties value presence to phase."

  sim.pico.tank.module.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case); SimulatesVersion 011 -> 012. Context-dependent: the device type lived on the referenced cac, not the component."
      "000":
        summary: "Initial Sema version"

  sim.plant.actuation:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  sim.plant.flux:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  sim.ready:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version."

  sim.relay.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (sim-marker relay component for simulated layouts)."

  sim.sensor.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (new sim-marker component for simulated layouts; structurally component.gt)."

  sim.timestep:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version."

  single.machine.state:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  single.reading:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  snapshot.spaceheat:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: "Baseline runtime version (no prior version tracked)."

  spaceheat.node.gt:
    latest_version: "303"
    versioning_strategy: "literal"
    versions:
      "303":
        summary: >
          - InPowerMetering: drop (routing is owned by the consuming transactive declaration, not the node)
          - InPowerMeteringRequiresNameplate axiom: drop
          - ActorClass: gw1.actor.class:012 -> 013 (in-place while staging)
      "302":
        summary: "- ActorClass: gw1.actor.class:011 -> 012"
      "301":
        summary: >
          - BoardComponentId: add as Optional
          - ActorClass: gw1.actor.class:009 -> 011
      "300":
        summary: "- ActorClass: string -> gw1.actor.class:009"
      "200":
        summary: "Baseline runtime version (no prior version tracked)."

  spaceheat.telemetry.quantity.projection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  synced.readings:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  synced.readings.bundle:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - Add LatePersistenceTimePeriodList: array of utc.iso8601.seconds arrays
          - Add OperatingStateSequenceList: array of operating.state.sequence:000
      "002":
        summary: "- ChannelReadingsList[]: inline object -> channel.readings.list.item:000"
      "001":
        summary: "Baseline runtime version (no prior version tracked)."

  synth.channel.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  ticklist.hall:
    latest_version: "101"
    versioning_strategy: "literal"
    versions:
      "101":
        summary: "Initial Sema version"

  ticklist.hall.report:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  ticklist.reed:
    latest_version: "101"
    versioning_strategy: "literal"
    versions:
      "101":
        summary: "Initial Sema version"

  ticklist.reed.report:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  weather:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version — like-for-like port of the legacy weather_service.py wire shape onto Sema."

  weather.forecast:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  web.server.component.gt:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "001":
        summary: "Initial Sema version (flattened from the scada WebServerComponentGt runtime; WebServer is an inline object)."

  zone.circuit.governance.cmd:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Governance command (FromHandle, ToHandle, Event, optional SetpointF, TriggerId, SendTimeUnixMs); SetpointIffThermostatic axiom."

enums:
  base.g.node.class:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  buffer.regulation.mode:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.heatcall.source:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.relay.pin:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.relay.state:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.valve.state:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.zone.call.source:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  day.of.week:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  fsm.report.type:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  g.node.instance.status:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  g.node.instance.transport:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  g.node.status:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gpio.sense.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gpm.from.hz.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.g.node.class:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.house0.primary.flow.source:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.market.product.name:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.weather.forecast.fidelity:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.actor.class:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "013":
        added_values:
          - "I2cDacWriter"
      "012":
        added_values:
          - "SimSensorActor"
          - "SimRelayActor"
      "011":
        added_values:
          - "I2cThermistorReader"
      "010":
        added_values:
          - "GpioSensor"
          - "I2cBus"
          - "I2cRelayBoard"
      "009": {}

  gw1.actuation.authority:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.device.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001":
        added_values:
          - "Gw108I2cRelay"
          - "Gw108GpioRelay"
          - "SamsungAE055FCYDCG"
          - "SamsungAE055FEYMCG"
          - "Gw108Adc"
          - "GridworksSimGw108"
      "000": {}

  gw1.emission.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.heat.call.interpretation:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.lc.top.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.leaf.ally.all.tanks.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.leaf.ally.buffer.only.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.local.control.all.tanks.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.local.control.buffer.only.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.local.control.standby.top.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.main.auto.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.quantity:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "002":
        added_values:
          - "WindSpeed"
      "001":
        added_values:
          - "Time"
      "000": {}

  gw1.seasonal.storage.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.service.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.system.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "002":
        added_values:
          - "DollarsX1000"
          - "MilesPerHourX1000"
          - "KilowattHoursX1000"
      "001":
        added_values:
          - "Seconds"
          - "SecondsX10"
      "000": {}

  heatcall.source:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  hz.calc.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  i2c.adc.channel:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  i2c.adc.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.dac.channel:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.dac.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.dac.vref:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.mux.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.operation:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001":
        added_values:
          - "WriteByte"
          - "ReadBytes"
      "000": {}

  log.level:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  market.price.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  market.quantity.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  market.type.name:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  relay.closed.or.open:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.energization.state:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.open.or.closed:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.wiring.config:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  setpoint.phase:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  spaceheat.make.model:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "008":
        added_values:
          - "GRIDWORKS__SIM_SENSOR"
          - "GRIDWORKS__SIM_RELAY_BANK"
      "007":
        added_values:
          - "SAIER__SENHZG1WA"
          - "DFROBOT__DFR0971_TIMES2"
          - "GRIDWORKS__TANKMODULE3"
          - "GRIDWORKS__GW101"
          - "GRIDWORKS__SCADA_GW108"
      "003": {}

  spaceheat.telemetry.name:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "007":
        added_values:
          - "BinaryState"
      "006": {}

  spaceheat.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  temp.calc.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  thermistor.data.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  thermostat.kind:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  valve.open.or.closed:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  zone.actuator.kind:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.call.circuit.event:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.call.circuit.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.call.source:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  zone.circuit.governance.event:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.circuit.governance.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.circuit.role:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.setpoint.source:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}
